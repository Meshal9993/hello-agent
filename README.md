# Hello Agent

Hello Agent is a full-stack starter project with a FastAPI backend and a Vue frontend.

## Project layout

- `backend/` — FastAPI application and server-side code.
- `frontend/` — Vue application and client-side code.

## Setup

Dependencies have intentionally not been installed yet. Add the backend and frontend tooling when the first implementation task defines the required versions and scripts.
